# Core Backend Framework
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.10.3
email-validator==1.3.1

# Authentication & Security
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
python-multipart==0.0.20

# Data Processing & Analysis (Python 3.13 compatible)
pandas==2.2.3
numpy==2.1.3

# Machine Learning (Python 3.13 compatible)
scikit-learn==1.5.2
xgboost==2.1.3
joblib==1.4.2

# HTTP & API Requests
requests==2.32.3

# PDF Generation
reportlab==4.2.5

# Production Server
gunicorn==23.0.0

# NOTE: TensorFlow removed to speed up Render builds # Core Backend Framework
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.10.3
email-validator>=2.0.0

# Authentication & Security
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
python-multipart==0.0.20

# Data Processing & Analysis (Python 3.13 compatible)
pandas==2.2.3
numpy==2.1.3

# Machine Learning (Python 3.13 compatible)
scikit-learn==1.5.2
xgboost==2.1.3
joblib==1.4.2

# HTTP & API Requests
requests==2.32.3

# PDF Generation
reportlab==4.2.5

# Production Server
gunicorn==23.0.0
psycopg[binary]==3.3.1

# MongoDB driver (for migrations and runtime)
pymongo==4.4.0
dnspython==2.4.2
# AWS SDK for backups
boto3>=1.28.0

# NOTE: TensorFlow removed to speed up Render builds
# Add back only if deploying LSTM models: tensorflow==2.18.0

# Add back only if deploying LSTM models: tensorflow==2.18.0
